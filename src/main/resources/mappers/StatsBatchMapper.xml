<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!--
    namespace는 Java Mapper 인터페이스의 전체 경로와 같아야 한다.
    그래야 StatsBatchMapper의 메서드 호출이 아래 SQL id와 연결된다.
-->
<mapper namespace="com.example.smartmessaging.mapper.StatsBatchMapper">

    <!--
        특정 날짜의 기존 message_stat 활성 row를 soft-delete 처리한다.

        DB unique index는 is_deleted = 0인 stat_date에만 적용되므로,
        기존 활성 row를 비활성화한 뒤 같은 날짜의 새 활성 row를 insert할 수 있다.
    -->
    <update id="softDeleteMessageStat">
        UPDATE message_stat
        SET
            is_deleted = 1,
            updated_at = SYSTIMESTAMP,
            updated_by = #{systemUserId}
        WHERE stat_date = #{statDate}
          AND NVL(is_deleted, 0) = 0
    </update>

    <!--
        send_history에서 하루치 발송 통계를 집계해서 message_stat에 insert 한다.

        message_stat.id는 Oracle identity 컬럼이므로 insert 컬럼 목록에서 제외한다.
        softDeleteMessageStat 이후 실행되므로, 같은 날짜를 재실행해도 활성 통계 row는 최신 1건만 남는다.
    -->
    <insert id="insertMessageStat">
        INSERT INTO message_stat (
            total_send_count,
            total_success_count,
            billing_cost,
            max_cost,
            stat_date,
            created_at,
            updated_at,
            created_by,
            updated_by,
            is_deleted
        )
        SELECT
            NVL(SUM(sh.total_target_count), 0),
            NVL(SUM(sh.success_count), 0),
            NVL(SUM(sh.actual_cost), 0),
            NVL(SUM(sh.estimated_cost), 0),
            #{statDate},
            SYSTIMESTAMP,
            SYSTIMESTAMP,
            #{systemUserId},
            #{systemUserId},
            0
        FROM send_history sh
        WHERE sh.status = 'SENT'
          AND TRUNC(sh.completed_at) = #{statDate}
          AND NVL(sh.is_deleted, 0) = 0
    </insert>

    <!--
        특정 날짜의 기존 message_stat_by_degree 활성 row를 soft-delete 처리한다.
    -->
    <update id="softDeleteMessageStatByDegree">
        UPDATE message_stat_by_degree
        SET
            is_deleted = 1,
            updated_at = SYSTIMESTAMP,
            updated_by = #{systemUserId}
        WHERE stat_date = #{statDate}
          AND NVL(is_deleted, 0) = 0
    </update>

    <!--
        send_attempt에서 하루치 차수/채널별 발송 통계를 집계해서 message_stat_by_degree에 insert 한다.

        조인하는 테이블의 역할:
        - send_history: 발송 묶음/캠페인 정보. 발송 상태와 완료일자를 기준으로 통계 대상 날짜를 고른다.
        - send_target: 해당 발송 묶음의 대상 고객 정보. send_history와 send_attempt를 연결하는 중간 테이블이다.
        - send_attempt: 대상 고객에게 실제 몇 차에 어떤 채널로 발송을 시도했는지 저장한다.
        - channel: 채널 정보와 채널별 단가를 저장한다.

        degree는 send_attempt.attempt_order를 사용한다.
        cost는 성공한 시도에 대해서만 channel.cost_per_msg를 합산한다.
    -->
    <insert id="insertMessageStatByDegree">
        INSERT INTO message_stat_by_degree (
            degree,
            send_count,
            success_count,
            stat_date,
            cost,
            channel_id,
            created_at,
            updated_at,
            created_by,
            updated_by,
            is_deleted
        )
        SELECT
            -- 몇 차 발송인지. 예: 1차, 2차
            sa.attempt_order,

            -- 해당 차수/채널로 발송을 시도한 전체 건수
            COUNT(sa.id),

            -- 해당 차수/채널 발송 시도 중 성공한 건수
            SUM(CASE WHEN sa.is_succeeded = 1 THEN 1 ELSE 0 END),

            -- 배치가 집계하는 기준 날짜
            #{statDate},

            -- 성공한 발송 시도에 대해서만 채널 단가를 합산한 비용
            NVL(SUM(CASE WHEN sa.is_succeeded = 1 THEN c.cost_per_msg ELSE 0 END), 0),

            -- 발송 시도에 사용된 채널
            sa.channel_id,
            SYSTIMESTAMP,
            SYSTIMESTAMP,
            #{systemUserId},
            #{systemUserId},
            0
        FROM send_attempt sa
        -- send_attempt는 send_target_id를 통해 발송 대상과 연결된다.
        INNER JOIN send_target st
                ON st.id = sa.send_target_id
               AND NVL(st.is_deleted, 0) = 0
        -- send_target은 send_history_id를 통해 발송 묶음과 연결된다.
        INNER JOIN send_history sh
                ON sh.id = st.send_history_id
               AND NVL(sh.is_deleted, 0) = 0
        -- channel은 채널 단가(cost_per_msg)를 계산하기 위해 연결한다.
        INNER JOIN channel c
                ON c.id = sa.channel_id
               AND NVL(c.is_deleted, 0) = 0
        -- 완료된 발송 묶음만 통계에 포함한다.
        WHERE sh.status = 'SENT'
          -- completed_at은 시간까지 포함하므로 TRUNC로 날짜만 비교한다.
          AND TRUNC(sh.completed_at) = #{statDate}
          AND NVL(sa.is_deleted, 0) = 0
        -- 차수와 채널별로 한 row씩 집계한다.
        GROUP BY sa.attempt_order, sa.channel_id
    </insert>

</mapper>
