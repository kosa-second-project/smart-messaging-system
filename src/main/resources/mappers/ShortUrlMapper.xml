<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.smartmessaging.mapper.ShortUrlMapper">

    <insert id="insertShortUrl" parameterType="ShortUrlVO">
        INSERT INTO short_url (
            id,
            original_url,
            send_target_id,
            is_clicked,
            clicked_at,
            is_converted,
            purpose,
            <include refid="Common.auditColumns"/>
        ) VALUES (
            #{id},
            #{originalUrl},
            #{sendTargetId},
            0,
            NULL,
            #{isConverted},
            #{purpose},
            <include refid="Common.auditInsertValues"/>
        )
    </insert>

    <select id="findById" resultType="ShortUrlVO">
        SELECT
            id,
            original_url,
            send_target_id,
            is_clicked,
            clicked_at,
            is_converted,
            purpose,
            <include refid="Common.auditColumns"/>
        FROM short_url
        WHERE id = #{id}
          AND NVL(is_deleted, 0) = 0
    </select>

    <update id="markClicked">
        UPDATE short_url
        SET is_clicked = 1,
            clicked_at = COALESCE(clicked_at, SYSTIMESTAMP),
            updated_at = SYSTIMESTAMP
        WHERE id = #{id}
          AND NVL(is_deleted, 0) = 0
    </update>

    <update id="markConverted">
        UPDATE short_url
        SET is_converted = 1,
            updated_at = SYSTIMESTAMP
        WHERE id = #{id}
          AND NVL(is_deleted, 0) = 0
    </update>
</mapper>
